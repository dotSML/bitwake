apiVersion: v1
kind: Service
metadata:
  name: neotorrent
  namespace: media
spec:
  selector:
    app: neotorrent
  ports:
    - name: http
      port: 80
      targetPort: webui
      protocol: TCP
